<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/canvas_bg">

        <!-- ── Top App Bar ── -->
        <LinearLayout
            android:id="@+id/top_bar_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/canvas_bg"
            android:elevation="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="20dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <!-- Hamburger button -->
            <FrameLayout
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="@drawable/bg_mode_pill"
                android:backgroundTint="@color/card_inset">
                <ImageView
                    android:id="@+id/menu_button"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center"
                    android:clickable="true"
                    android:focusable="true"
                    android:foreground="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_menu_bars"
                    app:tint="@color/accent_warm" />
            </FrameLayout>

            <!-- App name -->
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:layout_weight="1"
                android:fontFamily="@font/inter"
                android:letterSpacing="-0.02"
                android:text="SONA"
                android:textColor="@color/accent_warm"
                android:textSize="17sp"
                android:textStyle="bold" />

            <!-- Mode badge -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:background="@drawable/bg_mode_pill"
                android:backgroundTint="@color/accent_warm_xlight"
                android:gravity="center"
                android:paddingHorizontal="10dp"
                android:orientation="horizontal">

                <View
                    android:layout_width="5dp"
                    android:layout_height="5dp"
                    android:layout_marginEnd="5dp"
                    android:background="@drawable/bg_play_btn_warm"
                    android:backgroundTint="@color/accent_warm" />

                <TextView
                    android:id="@+id/status_badge"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/space_grotesk"
                    android:letterSpacing="0.06"
                    android:text="BIT-PERFECT"
                    android:textColor="@color/accent_warm"
                    android:textSize="9sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </LinearLayout>

        <!-- Fragment content area -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@id/bottom_navigation_container"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/top_bar_container" />

        <!-- Floating Bottom Nav: icon-only, no labels -->
        <FrameLayout
            android:id="@+id/bottom_navigation_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="32dp"
            android:layout_marginBottom="16dp"
            android:elevation="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bottom_navigation"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:background="@drawable/bg_bottom_nav_float"
                android:elevation="0dp"
                app:itemIconSize="24dp"
                app:itemIconTint="@color/bottom_nav_color"
                app:itemRippleColor="@color/accent_warm_xlight"
                app:labelVisibilityMode="unlabeled"
                app:itemActiveIndicatorStyle="@style/Widget.BottomNav.Indicator"
                app:menu="@menu/bottom_nav_menu" />
        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/canvas_bg"
        app:headerLayout="@layout/nav_header"
        app:itemIconTint="@color/accent_warm"
        app:itemTextColor="@color/text_primary_soft"
        app:itemShapeFillColor="@android:color/transparent"
        app:itemShapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Medium"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
